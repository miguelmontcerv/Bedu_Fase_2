#Reto 2: Trabajaremos con un data frame que viene incluido en R, en
 #este caso incluye información de autos

str(mtcars) #Nos describe el tipo de dato, el nombre y lo que contiene 
summary(mtcars) #Nos regresa un resumen por cada campo
head(mtcars) #Muestra los 10 primero sdatos de frame
View(mtcars) #Nos muestra en un mejor formato el data frame
